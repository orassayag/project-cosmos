# Master Run Ledger
Plan: docs/plans/add-ai.md

## Stage 1 — M0: move Vite app into client/ workspace (committed 2026-09-25T21:18:00Z)
**Files:** 86 `git mv` renames (index.html, public/, src/**, tsconfig.json, vite.config.ts → client/), package.json, package-lock.json, client/package.json, eslint.config.mjs, .gitignore
**What was built:** Root package.json is now an npm workspaces root (`["client"]`) with orchestration scripts + shared tooling only; React/Vite deps live in `client/package.json` (`@project-cosmos/client`). `client/vite.config.ts` resolves the versions ledger via `fileURLToPath(new URL('../versions/', import.meta.url))` (cwd-independent). eslint targets `client/src` and ignores `**/dist/`; .gitignore adds client/dist/, server/dist/, **/tsconfig.tsbuildinfo. build/lint/typecheck pass.
**Key decisions:** `dev` and `dev:client` both run the client workspace until stage 6 swaps `dev` to `vercel dev`. `build`/`typecheck` fan out with `--workspaces` and no `--if-present` — stage 3 MUST give `server` its own `build` + `typecheck` scripts. Lint stays a single root `eslint .`. `validate`, `sync*`, `fresh` scripts unchanged at root. vercel.json untouched until stage 6.
**Known broken until stage 2:** `npm run validate` fails (drift-sync imports `../../src/...`); `scripts/fresh-start.mjs` still resolves `<root>/src/scenarios/`; `npx tsc -p drift-sync --noEmit` baseline was not captured (permission denied) — stage 2 should capture it.
**User overrides during review:** none.

## Stage 2 — M0: repoint drift-sync + fresh-start to client/src (committed 2026-09-26T00:00:00Z)
**Files:** drift-sync/README.md, drift-sync/scripts/{apply-edits,bootstrap-state,diff-repo,sync-nightly,sync,validate}.ts, drift-sync/scripts/lib/{agent,cosmos-context}.ts, drift-sync/tsconfig.json, scripts/fresh-start.mjs
**What was built:** Every root-relative `src/…` consumer outside the client now points at `client/src/…` (imports, prompts, tool descriptions, README). `WRITABLE_PATHS` is `client/src/scenarios/` in **both** apply-edits.ts:87 and sync-nightly.ts:213. drift-sync/tsconfig.json extends `../client/tsconfig.json` (root tsconfig no longer exists). `npm run validate`, build, lint pass; a deliberate bad id makes validate exit 1.
**Key decisions:** No new root tsconfig — drift-sync inherits client's compiler options (incl. `vite/client` types + DOM libs). `sync.ts` `repoRoot = path.resolve(here,'..')` → `drift-sync/` is pre-existing and unrelated; left alone.
**Still open:** `npx tsc -p drift-sync --noEmit` NOT run (permission denied) — stage 5 gate must run it. Plan's `npm run sync -- --dry-run` does not exist; the real credential-free check is `npm run sync:bootstrap -- --dry-run` — stage 5 should use that. Pre-existing: `client/src/scenarios/steps/core.ts` (`CORE_STEPS`) is not in the `data.ts` barrel, so validate never sees its steps — out of scope, untouched.
**User overrides during review:** none.

## Stage 3 — M0: server/ workspace skeleton + committed cosmos-map snapshot (committed 2026-09-26T00:00:00Z)
**Files:** package.json, package-lock.json, eslint.config.mjs, server/package.json, server/tsconfig.json, server/scripts/tsconfig.json, server/scripts/snapshot-map.ts, server/src/generated/cosmos-map.json, drift-sync/scripts/{validate,apply-edits,sync-nightly}.ts
**What was built:** `@project-cosmos/server` workspace (ESM, no runtime code, no deps) with `build`/`typecheck`/`snapshot` scripts; root workspaces `["client","server"]` + root `snapshot` script. `server/scripts/snapshot-map.ts` builds a deterministic compact JSON (domains, teams, services w/ calls/publishes/consumes, topics w/ producers/consumers, scenarios + incidents with ordered steps; no layout/colour/payloads) from the `client/src/scenarios/data.ts` barrel + `owners.ts` → `server/src/generated/cosmos-map.json`. `validate.ts` has a `stale-snapshot` check (message: `cosmos-map.json is stale — run npm run snapshot`; also fires when missing). apply-edits runs `npm run snapshot` after the agent and before diff capture; snapshot path added to `WRITABLE_PATHS` in apply-edits AND sync-nightly; agent may run `npm run snapshot` before validate. ESLint Node-globals block covers `server/**/*.ts`. build/typecheck/lint/validate pass; stale check proven for hand-edit, deletion, and unsnapshotted data edit.
**Key decisions:** Two server tsconfigs — `server/tsconfig.json` is NodeNext (all `server/src` code must use `.js` import suffixes); `server/scripts/tsconfig.json` extends it with Bundler resolution because client source has extensionless imports (TS2835 under NodeNext). Root server tsconfig includes `src/**/*.json` to avoid TS18003 before any `.ts` exists. Filename stays kebab-case `snapshot-map.ts`. Snapshot relationships derive from scenario STEPS only (not incidents); kafka consumer = `through ?? to`. Orphaned `steps/core.ts` excluded (pre-existing, not in barrel).
**Still open:** `npx tsc -p drift-sync --noEmit` and `npx eslint <server file>` still permission-denied — stage 5 gate must run both. Snapshot is single-line JSON (one huge diff line in drift PRs); switch to 2-space indent in `serializeCosmosMap` if reviewable diffs are wanted. Lint has 2 pre-existing exhaustive-deps warnings (AskPanel.tsx, Map.tsx).
**User overrides during review:** none.

## Stage 4 — M0: repoint skills to client/src (committed 2026-09-26T00:00:00Z)
**Files:** .claude/skills/add-scenario/SKILL.md, .claude/skills/add-service/SKILL.md, .claude/skills/update/SKILL.md, skills/add-scenario/SKILL.md, skills/add-service/SKILL.md
**What was built:** Every Project Cosmos `src/…` path in the map-growing skills now reads `client/src/…` (incl. description frontmatter). add-scenario/add-service verify steps run `npm run typecheck`, `npm run snapshot`, `npm run validate`, `npm run build` (old `npx tsc -b --noEmit` has no root tsconfig). "What you'll touch" tables list `server/src/generated/cosmos-map.json` as regenerated by `npm run snapshot`, committed with data edits. `skills/add-*` stay byte-identical to `.claude/skills/add-*`.
**Key decisions:** Also repointed `.claude/skills/update/SKILL.md` (2 stale refs; its validate step now runs `npm run snapshot` first). Bare filenames next to full `client/src/…` paths left unchanged.
**User overrides during review:** none.

## Stage 5 — M0: repoint CLAUDE.md, README, CONTRIBUTING; M0 verification gate (committed 2026-09-26T00:00:00Z)
**Files:** CLAUDE.md, README.md, CONTRIBUTING.md
**What was built:** Contributor docs now point at `client/src/…` and list the real root scripts (`dev`, `dev:client`, `build`, `typecheck`, `lint`, `snapshot`, `validate`); `validate` documented as checking snapshot freshness, `npm run snapshot` required after data edits. `cosmos-sync.yml` needed no change (no `src/` path). M0 gate green: grep gate, build, typecheck, lint (2 pre-existing warnings), `tsc -p drift-sync`, eslint on snapshot-map.ts, validate + corruption proofs (bad step id, stale snapshot), 390px + desktop runtime parity vs main.
**Key decisions:** Historical records (plans, ledger, stage reports) with old paths left as-is. Bare `npx`/`./node_modules/.bin` is permission-denied — use `npm exec -- <bin>`. `npm run sync -- --dry-run` does not exist; `npm run sync:bootstrap -- --dry-run` resolves the map but exits 1 (no sibling repos cloned — environment limit). No `test` script exists yet (stage 7 adds it); `dev` is not `vercel dev` yet.
**Still open:** Deep link `step=3` loads as `step=1` on both main and branch (pre-existing). A stale root `vite` on :5173 may be running — use another port for runtime checks.
**User overrides during review:** none.

## Stage 6 — Hosting: Vercel Services, Pages redirect, env example, README AI block (committed 2026-09-26T00:00:00Z)
**Files:** vercel.json, .github/workflows/pages.yml, pages-redirect/index.html, server/.env.example, README.md, .gitignore
**What was built:** `vercel.json` uses Services — `client` (root `client`, SPA rewrite) and `server` (root `server`); `/api/(.*)` → server, rest → client; `git.deploymentEnabled: false` kept. Pages workflow no longer builds: it copies `pages-redirect/index.html` → `404.html` and publishes a static redirect to `https://project-cosmos-six.vercel.app` preserving path (minus `/project-cosmos`), query, hash; 3s meta-refresh no-JS fallback to root. `server/.env.example` lists `AI_GATEWAY_API_KEY`, `AI_COOKIE_SECRET`. README gained "Run with AI locally" (`vercel link` / `vercel env pull` / `vercel dev`, or `cp server/.env.example server/.env` + `vercel dev -L`; `npm run dev:client` = no-AI path). `.gitignore` re-allows `!.env.example` after the trailing `.env*`.
**Key decisions:** Root `dev` still plain Vite — `vercel dev -L` detects both services but requests hang while `server` has no entrypoint; README uses `vercel dev` directly until `dev` switches. `client/vite.config.ts` `BASE_PATH` comment ("set by the Pages workflow") is now stale — out of scope, left.
**Still open:** Switch root `dev` to `vercel dev` (+ README line → `npm run dev`) once the Hono entrypoint exists (stage 11) and re-test `vercel dev -L`. Unverified whether root `.env.local` from `vercel env pull` reaches the `server` service or `server/.env` is needed — check with the first real route.
**User overrides during review:** none.

## Stage 7 — Vitest harness in client + server, root test fan-out, CI npm test step (committed 2026-09-26T00:00:00Z)
**Files:** package.json, package-lock.json, client/package.json, client/vitest.config.ts, client/src/testing/vitestSetup.ts, client/src/components/__tests__/PanelCloseButton.test.tsx, server/package.json, server/src/__tests__/cosmosMap.test.ts, .github/workflows/validate-on-pr.yml
**What was built:** Root `npm test` = `npm run test --workspaces` (no `--if-present`, no `passWithNoTests`). Client: `vitest run` with jsdom + RTL 16 (`@testing-library/dom` peer); `client/vitest.config.ts` merges over `vite.config.ts` (same React plugin + `__APP_VERSION__`), include `src/**/__tests__/**/*.test.{ts,tsx}`, setup `src/testing/vitestSetup.ts` (`afterEach(cleanup)` — globals are off, so RTL won't auto-clean). Server: `vitest run`, no config (defaults find `src/__tests__/*.test.ts`); JSON imported `with { type: 'json' }`. CI `validate-on-pr.yml` runs `npm test` after Typecheck drift-sync, before Validate. Both sample tests proven non-vacuous.
**Key decisions:** Test files live inside the app tsconfigs and import `vitest`/RTL explicitly — no globals, no `jest-dom` (add later if DOM matchers needed). No `matchMedia` stub — `useViewport`/`Map`/`BrandStarfield` already guard; phone-viewport tests use `vi.stubGlobal('matchMedia', …)`. `vite.config.ts` untouched. jsdom 30 needs Node `^22.22.2 || ^24.15 || >=26`; root `engines: >=20` left for stage 11 to tighten.
**User overrides during review:** none.

## Stage 8 — M1: shared .lc-status-dot, useAiConnection, AskAgent Search/bot light/Connect-Disconnect (committed 2026-09-26T00:00:00Z)
**Files:** client/src/hooks/useAiConnection.ts, client/src/components/AskAgent.tsx, client/src/components/__tests__/AskAgent.test.tsx, client/src/components/DriftFooter.tsx, client/src/styles/app.css, client/src/App.tsx
**What was built:** `.lc-drift-footer-dot` → shared `.lc-status-dot` with `--on` (green + `lc-status-pulse`), `--off` (red), `--unknown` (grey); DriftFooter uses `lc-status-dot--on`. `useAiConnection()` (in `client/src/hooks/`): one `GET /api/ai/status` on mount (aborted on unmount) → `{ status: 'unknown'|'connected'|'disconnected', provider, connect(provider, apiKey), disconnect() }`; any failure/404/503/bad body → `disconnected`, never throws, no console. AskAgent: "Go!" → **Search**; 🤖 + status dot right of input (aria-label/tooltip "AI agent connected (Claude)" / "No AI agent connected" / "Checking for an AI agent"); expanded footer shows **Connect AI Agent** or **Disconnect AI Agent** left of Search (`onMouseDown`+`preventDefault`). New props `aiStatus`, `aiProvider`, `onConnectRequest`, `onDisconnect`. App calls the hook and passes props; `onConnectRequest` is an EMPTY callback — stage 9 replaces it with `overlay.open(OVERLAY.connect)`.
**Key decisions:** `connect()` → `Promise<ConnectResult>`, `ConnectResult = { ok: true; provider } | { ok: false; errorCode: 'INVALID_KEY'|'AI_NOT_CONFIGURED'|'NETWORK_ERROR'|'UNEXPECTED_RESPONSE' }`; never throws; state updates only on success — the stage-9 modal shows the error on `ok:false` and stays open. `disconnect()` → `Promise<boolean>`, flips only when the server confirms (httpOnly cookie). Hook lives in App; AskAgent is presentational. `unknown` shows neither button. `AI_PROVIDER_LABELS` (anthropic → "Claude", openai → "OpenAI") exported from the hook module. Connect blurs/collapses the card before `onConnectRequest`. Verified at 390×844, 844×390, 1440×900 via scratchpad Playwright on a non-5173 port.
**Still open:** No unit test for `useAiConnection` itself — add `client/src/hooks/__tests__/useAiConnection.test.ts` in stage 9 or 12 if within ceiling.
**User overrides during review:** none.

## Stage 9 — M1: ConnectAgentModal + OVERLAY.connect + responsive priority + test (committed 2026-09-26T00:42:00Z)
**Files:** client/src/components/ConnectAgentModal.tsx, client/src/components/__tests__/ConnectAgentModal.test.tsx, client/src/overlays/OverlayManager.tsx, client/src/App.tsx, client/src/styles/app.css, client/src/styles/responsive.css
**What was built:** `OVERLAY.connect = 'connect-agent'`. `ConnectAgentModal` (portaled to `<body>`) reads `useOverlay()` itself — visible only while `overlay.isOpen(OVERLAY.connect)`; props are just `currentProvider` and `onConnect` (from the same `useAiConnection()` instance in App, so the AskAgent dot turns green on success). Claude/OpenAI segmented picker (`role="radiogroup"`), password key field (16px, `autoComplete="off"`), per-provider "Get a key" link, Connect → "Connecting…" + form disabled, inline error, stays open on failure, closes on success. "Replaces current provider" note when already connected. App `onConnectRequest` → `overlay.open(OVERLAY.connect)`. Own top-right `.lc-help-close`; joins the "One card at a time" block as highest priority via `body:has(.lc-connect-overlay) .lc-stage :is(...)`.
**Key decisions:** Reuses help-modal shell classes (`lc-help-overlay lc-help-modal lc-help-close`), so App's global Esc "reset galaxy" guard already skips; modal's own Esc closes it. Error map: `INVALID_KEY` → "That key didn't work — check it and try again", `AI_NOT_CONFIGURED` → "AI answers aren't available on this site right now.", `NETWORK_ERROR` → check connection, `UNEXPECTED_RESPONSE` → generic retry. Key trimmed; state unmounts on close (key never persists). Backdrop/Esc/× ignored while connecting. **Stage 10 opens it with `overlay.open(OVERLAY.connect)` — no props/state needed.** Verified 390×844, 844×390, 1440×900 with mocked `/api/ai/*`.
**Still open:** ~325 hand-written LOC vs 250 ceiling (mostly modal CSS in app.css) — accepted as is. `useAiConnection` unit test still deferred → stage 12.
**User overrides during review:** none (approved via `stage 10 start`).

## Stage 10 — M1: AskPanel connect prompt + starter question chips (committed 2026-09-26T01:10:00Z)
**Files:** client/src/components/AskAgent.tsx, client/src/components/AskPanel.tsx, client/src/components/__tests__/AskAgent.test.tsx, client/src/components/__tests__/AskPanel.test.tsx, client/src/App.tsx, client/src/styles/app.css, client/src/styles/responsive.css
**What was built:** AskPanel shows an underlined "Connect an AI agent for real answers." button under the joke answer once `phase === 'done'` and only when status is `disconnected` (hidden while `unknown`/connected); click → `overlay.open(OVERLAY.connect)`. AskAgent shows three starter chips (`STARTER_QUESTIONS`, exported) when expanded with an empty field; chip click calls `onAsk(starter)` directly (avoids stale `question` state), uses `onMouseDown`+`preventDefault` so the card doesn't collapse first. Card height grows only while chips show (`.lc-ask--starters`: desktop 180px, phone portrait 230px, landscape 190px); touch targets 40px.
**Key decisions:** AskPanel stays presentational — new props `showConnectPrompt`, `onConnectRequest`; App passes `aiConnection.status === 'disconnected'` from the single `useAiConnection()` instance. Phone-only rules in `responsive.css`. On phones the Connect modal stacks over the answer panel (closing restores it); on desktop opening the modal closes the answer panel (one overlay at a time). Verified 390×844, 844×390, 1440×900 with mocked `/api/ai/status`.
**Still open:** 7 files vs ceiling 6 (new AskPanel.test.tsx) — accepted. `useAiConnection` unit test still deferred → stage 12.
**User overrides during review:** none (approved via `stage 11 start`).

## Stage 11 — M2: Hono app, lazy AI config, structured logger, cookieCrypto (committed 2026-09-26T02:00:00Z)
**Files:** package.json, package-lock.json, server/package.json, vercel.json, server/src/app.ts, server/src/config.ts, server/src/logger.ts, server/src/cookieCrypto.ts, server/src/__tests__/cookieCrypto.test.ts
**What was built:** `server/src/app.ts` — Hono app with `basePath('/api')`, JSON `404 NOT_FOUND` / `500 INTERNAL_ERROR` handlers; default export is also the Vercel entry (`vercel.json` pins `"entrypoint": "src/app.ts"` on the `server` service — without it `vercel dev` doesn't detect the server). `config.ts`: `getCookieSecret(): Buffer | null` (read per call; missing/not-32-bytes → null + one-time error log `AI_NOT_CONFIGURED` / `AI_COOKIE_SECRET_INVALID`, module-level flag), `getGatewayApiKey(): string | null` (no warn). `logger.ts`: structured JSON logger that allowlists `{ level, scope, message, errorCode, provider, noPHI: true, time }` — the only `console.*` user. `cookieCrypto.ts`: AES-256-GCM seal/open of `{ provider, apiKey }` as `base64url(iv ‖ ciphertext ‖ authTag)`; open returns `AiCookiePayload | null`, never throws, warns `AI_COOKIE_REJECTED`. Root `engines.node >=22`; `hono@^4.13.9` server dep.
**Key decisions:** Stage 12 registers routes as `app.post('/ai/connect', …)` on the default-exported app (→ `/api/ai/connect`); tests use `app.request('/api/ai/…')`. Cookie constants in `cookieCrypto.ts`: `AI_COOKIE_NAME = 'cosmos_ai'`, `AI_COOKIE_OPTIONS` (httpOnly, secure, `sameSite: 'Strict'`, `path: '/api/ai'`, `maxAge: 2592000`); disconnect with `{ ...AI_COOKIE_OPTIONS, maxAge: 0 }` or `deleteCookie` with the same path. Null decrypt → treat as not connected and clear cookie. `getCookieSecret()` null → `503 { errorCode: AI_NOT_CONFIGURED }`. A `config.test.ts` asserting "one error" needs `vi.resetModules()` between cases. `AI_PROVIDERS` / `AiProvider` temporarily live in `logger.ts` — **stage 12's `ConnectRequestSchema` (`z.enum(AI_PROVIDERS)`) should become canonical or move them.** `app.onError` logs only `INTERNAL_ERROR` (plan's "errorCode/provider/noPHI only" rule, deliberate trade vs. log-with-context). Non-vacuous proof via two mutations; `vercel dev -L` verified both services on one port.
**Still open:** 8 hand-written files vs ceiling 6 — accepted. Root `"dev": "vercel dev"` + README line (stage 6 carry-over) still not done — verified feasible; unverified whether `.env.local` from `vercel env pull` reaches the server service (check with stage 12's first cookie route). `server/package.json` `build` (`tsc --noEmit`) kept — confirm harmless on first preview deploy. `useAiConnection` unit test still deferred → stage 12.
**User overrides during review:** none (approved via `stage 12 start`).

## Stage 12 — M2: connect / disconnect / status routes + ConnectRequestSchema + key check (committed 2026-09-26T03:00:00Z)
**Files:** package-lock.json, server/package.json, server/src/app.ts, server/src/providerKeyCheck.ts, server/src/schemas/connectRequestSchema.ts, server/src/__tests__/connectRoute.test.ts, server/src/__tests__/statusRoute.test.ts, server/src/__tests__/config.test.ts
**What was built:** Routes registered directly on the default-exported app in `app.ts` (no sub-router — stage 17 adds `/ai/ask` the same way). `POST /api/ai/connect`: secret → body (`ConnectRequestSchema`) → `checkProviderKey` → seal into `cosmos_ai` cookie. `POST /api/ai/disconnect`: `deleteCookie(context, AI_COOKIE_NAME, AI_COOKIE_OPTIONS)`, works without `AI_COOKIE_SECRET`. `GET /api/ai/status`: re-checks the stored key every call; 401 → clear cookie + `{ connected:false, reason:'KEY_REVOKED' }`; provider outage keeps user connected. `zod@^4.6.5` now a direct server dep.
**Key decisions:** Response shapes: connect `200 { connected:true, provider }`; `400 { errorCode:'INVALID_KEY' }`; `400 { errorCode:'INVALID_REQUEST', field, message }` (`field` ∈ provider/apiKey/body); `502 { errorCode:'PROVIDER_UNAVAILABLE' }`; `503 { errorCode:'AI_NOT_CONFIGURED' }`; disconnect `200 { connected:false }`. `checkProviderKey(provider, apiKey): Promise<'valid'|'invalid'|'unavailable'>` in `server/src/providerKeyCheck.ts` — only 401 = invalid; network/5s timeout/other non-2xx = unavailable; global `fetch` (tests: `vi.stubGlobal('fetch', …)`); Anthropic `x-api-key` + `anthropic-version: 2023-06-01`, OpenAI `Authorization: Bearer`; body cancelled unread. `ConnectRequestSchema` (+ `type ConnectRequest`) in `server/src/schemas/connectRequestSchema.ts`: `provider` = `z.enum(AI_PROVIDERS)`, `apiKey` trimmed, non-empty, max 512. Logs: `INVALID_KEY` warn, `KEY_REVOKED` info, `PROVIDER_UNREACHABLE`/`PROVIDER_UNAVAILABLE` warn; tests assert the key never appears in console output. `config.test.ts` uses `vi.resetModules()` + dynamic `import('../app.js')` per case; the `ask` → 503 case is left for stage 17 (note at top of file).
**Still open:** 8 files vs ceiling 6 — accepted. `AI_PROVIDERS`/`AiProvider` still in `server/src/logger.ts` (small follow-up: move into the schema file, re-point logger/cookieCrypto imports). `vercel dev -L` smoke not run (another `vercel dev`, PID 38141, held the lock) — still unverified whether `vercel env pull` values reach the server service. Provider 403/5xx on connect → 502, not `INVALID_KEY` (plan names only 401). `useAiConnection` unit test and root `"dev": "vercel dev"` + README line still deferred.
**User overrides during review:** none (approved via `stage 13 start`).

## Stage 13 — §5: route.ts decision, localRelevance, OFF_TOPIC_ANSWERS + tests (committed 2026-09-26T04:00:00Z)
**Files:** server/src/agent/route.ts, server/src/agent/localRelevance.ts, server/src/agent/offTopicAnswers.ts, server/src/agent/types/cosmosMapSnapshot.ts, server/src/agent/__tests__/route.test.ts, server/src/agent/__tests__/localRelevance.test.ts
**What was built:** Pure question router, not yet wired to a route. `decideRoute(input: RouteInput, snapshot: Pick<CosmosMapSnapshot,'scenarios'|'incidents'>, pickIndex?)` → `RouteDecision` = `{ kind:'offTopic'; answer }` | `{ kind:'directAction'; answer; action:{ type:'playScenario'; scenarioId } }` | `{ kind:'agent'; hints:{ intent: Intent|null; targetScenarioId: string|null } }`. `RouteInput` = `{ source:'classifier'; classification: Classification }` | `{ source:'localRelevance'; onTopic: boolean }` — the fallback can never yield a direct action. `localRelevance(question, snapshot): boolean` — whole-word, case/separator-insensitive match over service/topic/domain/team/scenario/incident ids+names plus a fixed architecture word list; terms <3 chars skipped; terms cached per snapshot in a `WeakMap`. `OFF_TOPIC_ANSWERS: readonly string[]` (8 lines, plan's example verbatim). `CosmosMapSnapshot` is a minimal readonly structural type; the `cosmos-map.json` import (`with { type: 'json' }`) satisfies it.
**Key decisions:** `INTENTS = ['explainFlow','findService','playScenario','incident','ownership'] as const`; `OFF_TOPIC_THRESHOLD = 0.35` (off-topic is `<`), `DIRECT_ACTION_THRESHOLD = 0.6` (direct is `>=`). **Stage 14's `classify.ts` must map JEV output into `Classification { onTopicProbability; intent; targetScenarioId: string|null; targetScenarioProbability }`** — JEV `none` → `targetScenarioId: null`; `route.ts` carries no `ai`/JEV types. Target ids resolve across **scenarios + incidents** (plan's "8 today" = 5 + 3) — stage 14 should build JEV choices from both. Unknown target id → hint dropped, no direct action. Direct-action text is `Playing *<title>* for you ▶` (literal from plan). Non-vacuous proof via 3 mutations (both thresholds, whole-word match).
**Still open:** How stage 17 renders the `*Title*` markdown in the stream (unverified). Carry-overs from stage 12 still open: move `AI_PROVIDERS`/`AiProvider` out of `logger.ts`; `vercel dev -L` env smoke; `useAiConnection` unit test; root `"dev": "vercel dev"` + README line.
**User overrides during review:** none (approved via `stage 14 start`).
