# Master Run Ledger
Plan: docs/plans/demo-plan.md

## Stage 1 — Demo step types and demo-mode URL parsing (committed 2026-09-26T09:14:31Z)
**Files:** client/src/demo/types.ts, client/src/demo/demoMode.ts, client/src/demo/__tests__/demoMode.test.ts
**What was built:** `types.ts` — full `DemoStep` discriminated union (all §1/§7 kinds, each with `durationMs`, optional `caption`/`target`), `DemoTarget` union, `DemoActions` callback interface, `DemoAiStatus`, `DemoScriptedAnswer`, `DemoMode`/`DemoModeName`. `demoMode.ts` — `readDemoMode` (only `ai`/`all`, speed clamped 1–8, default 1; bad value → null) and `shouldShowIntro(demoMode, storage, hasDeepLink=false)` (never writes storage); exports `INTRO_SEEN_STORAGE_KEY`.
**Key decisions:** `DEMO_TARGETS` / `DEMO_STEP_KINDS` are runtime `as const` arrays; `DEMO_STEP_ACTIONS: Record<DemoStepKind, (keyof DemoActions)[]>` maps kinds to callbacks (for §9 coverage tests). Target ids for stage 10: `intro-start`, `domain-shopping|fulfillment|engagement`, `ask-input`, `ask-search`, `connect-open`, `connect-provider-anthropic|openai`, `connect-provider-key`, `connect-jev-key`, `connect-submit`, `playback-play`, `playback-step-back`, `playback-step-forward`, `legend-ownership`. `toggleLegend{isVisible}` sets explicitly. `ask` kind drives `setSearchPressed` + `ask`; `connect` drives `setAiStatus` (disconnected → connecting → connected). `DemoAiStatus` is separate from app `AiConnectionStatus` (stage 3 maps `connecting` → modal `isBusy`). `DemoScriptedAnswer { text; thinkingMs; wordMs; actions? }` shared by stages 6/7. `demo=all` always shows intro. Stage 8 should pass `hasDeepLink = initial.scenario != null || initial.incident != null || initial.domain != null`.
**User overrides during review:** none.

## Stage 2 — Abortable demo runner with speed scaling (committed 2026-09-26T09:19:14Z)
**Files:** client/src/demo/runDemo.ts, client/src/demo/__tests__/runDemo.test.ts
**What was built:** `runDemo(script, actions, { signal, speed, onStepStart? })` plays steps sequentially through `DemoActions` only, divides durations by `speed`, resolves `'done'`/`'aborted'` (never throws on abort). Exports `sleep(ms, signal)` (clears timer + listener), `scaledDuration(ms, speed)` (for stage 9 animations), and `abortOnTrustedInput(controller, target = window)` (aborts on trusted `pointerdown`/`keydown`, returns cleanup — for stage 8).
**Key decisions:** Call-then-sleep: a step's actions fire at its start, then `durationMs` elapses. `connect` = `setAiStatus('connecting')` → wait → `'connected'`. `ask` = `setSearchPressed(true)` → `min(150, durationMs)` → `setSearchPressed(false)` + `ask(question)` → rest of duration. `onStepStart?(step, stepIndex)` added for stage 9's pointer/caption (runner ignores `target`/`caption`). Abort mid-step skips the closing actions, so stage 8's teardown must reset pressed state and drop the fake AI connection. Non-finite/non-positive speed throws `RangeError`. jsdom `Event.isTrusted` can't be overridden, so `abortOnTrustedInput` takes a `Pick<EventTarget, …>` target and tests pass a fake one.
**User overrides during review:** none.

## Stage 3 — Fake AI connection and disable switch for the real one (committed 2026-09-26T09:23:46Z)
**Files:** client/src/hooks/useAiConnection.ts, client/src/hooks/__tests__/useAiConnection.test.ts, client/src/demo/useDemoAiConnection.ts, client/src/demo/__tests__/useDemoAiConnection.test.ts
**What was built:** `useAiConnection({ enabled })` (default `true`) — when `false`, skips `/api/ai/status` and reports `disconnected`/no provider; re-enabling re-runs the status check. `useDemoAiConnection()` returns an `AiConnection`-compatible fake plus `isConnecting` and `setDemoStatus` (stable `useState` setter); provider always `anthropic` (`DEMO_AI_PROVIDER` exported); `connect`/`disconnect` never touch the network.
**Key decisions:** Mapping `DemoAiStatus` → fields: `disconnected` → `status:'disconnected'`, provider `null`, `isConnecting:false`; `connecting` → `status:'disconnected'`, provider `null`, `isConnecting:true`; `connected` → `status:'connected'`, provider `anthropic`, `isConnecting:false`. **Stage 4:** `ConnectAgentModal` keeps `isBusy` as internal `useState` — needs a new prop (e.g. `isDemoBusy`) so App can pass `demoAi.isConnecting`. **Stage 8:** call both hooks every render: `realAi = useAiConnection({ enabled: !isDemoActive })`, `demoAi = useDemoAiConnection()`, pass `isDemoActive ? demoAi : realAi`; `DemoActions.setAiStatus = demoAi.setDemoStatus`; on end/abort call `demoAi.setDemoStatus('disconnected')` and clear `isDemoActive`. Real hook shows last-known status (possibly `unknown`) after re-enable until the new check returns. Plan's "no `fetch` during full `ai` script" runner test deferred to stage 7 (needs the `demo=ai` script).
**User overrides during review:** none.

## Stage 4 — Demo-controlled Connect window with JEV key field (committed 2026-09-26T10:12:13Z)
**Files:** client/src/components/ConnectAgentModal.tsx, client/src/components/__tests__/ConnectAgentModal.test.tsx, client/src/demo/types.ts
**What was built:** `ConnectAgentModal` takes optional `demo?: DemoConnectState` (`{ provider, providerKey, jevKey, showJevField, isBusy }`, in `demo/types.ts`). With `demo`: provider/key/busy come from the prop, inputs `readOnly`, provider buttons inert, a site-owner-only password field "Vercel AI Gateway key (JEV, site owner)" shows when `showJevField`, submit never calls `onConnect`. Without `demo`: unchanged, no JEV field.
**Key decisions:** Internal state renamed `selectedProvider`/`typedKey`/`isSubmitting`; rendered values are `demo?.x ?? internal`. Submit button stays enabled in demo mode when key non-empty so stage 10's pointer can click it; `handleSubmit` returns early, runner drives connecting → connected via §3. **Stage 8:** App builds `demo` from runner state — `provider` from `pickProvider`, `providerKey`/`jevKey` from `setConnectField` (both start `''`), `showJevField: true`, `isBusy: demoAi.isConnecting` — and passes it only while the demo is active. Caption text is not rendered in the modal (stage 9's caption bar).
**User overrides during review:** none.

## Stage 5 — Demo-driven Ask box (committed 2026-09-26T10:15:21Z)
**Files:** client/src/components/AskAgent.tsx, client/src/components/__tests__/AskAgent.test.tsx, client/src/styles/app.css
**What was built:** `AskAgent` takes optional `demoQuestion`, `demoExpanded`, `demoSearchPressed`. With `demoQuestion` set the input is read-only, shows that text, focus expands but doesn't clear it, Enter/Escape inert, starter questions hidden. `demoExpanded` overrides expanded state. `demoSearchPressed` adds `lc-ask-go--pressed` (new rule in `app.css`, inset shadow, all viewports). No demo props → unchanged.
**Key decisions:** Same pattern as stage 4 — internal state `isExpandedInternal`/`typedQuestion`, rendered `demoX ?? internal`. Demo mode = `demoQuestion !== undefined` — **stage 8 must pass `''` (not `undefined`) at the start of a `type` step.** Pressed style uses box-shadow, not `transform` (entrance animation holds `transform` with `forwards`). Search button renders only while expanded — **stage 8 must keep `demoExpanded: true` through the `ask` step.**
**User overrides during review:** none.

## Stage 6 — AskPanel scripted answer (committed 2026-09-26T10:18:58Z)
**Files:** client/src/components/AskPanel.tsx, client/src/components/__tests__/AskPanel.test.tsx
**What was built:** `AskPanel` takes optional `scriptedAnswer` (`DemoScriptedAnswer` from `demo/types.ts`). When set: thinking dots for `thinkingMs`, then words revealed one per `wordMs`; `onAnswerStart` + every action fire once together at `thinkingMs`; no fetch (even with `isAiConnected`), no joke, no Connect prompt. All timers scheduled up front and cleared on unmount / prop change. No prop → unchanged.
**Key decisions:** Scripted mode = `scriptedAnswer !== undefined`, overrides the live path (`isLive` false). Reused `DemoScriptedAnswer.actions?: AskAction[]` unchanged. **Stage 8: effect is keyed on `scriptedAnswer` object identity — keep it referentially stable (state, not an inline literal) or each re-render restarts the answer.**
**User overrides during review:** none.

## Stage 7 — Scripted AI demo and fixed answer (committed 2026-09-26T10:23:08Z)
**Files:** client/src/demo/scriptedAnswer.ts, client/src/demo/__tests__/scriptedAnswer.test.ts, client/src/demo/scripts.ts, client/src/demo/__tests__/scripts.test.ts
**What was built:** `scriptedAnswer.ts` exports `DEMO_QUESTION`, `DEMO_ANSWER_SERVICE_IDS` (7 services: storefront, api-gateway, orders, payments, inventory, realtime-hub, notifications) and `DEMO_SCRIPTED_ANSWER` (module-level constant, 67 words, 1.5s thinking + 90ms/word ≈ 7.5s, highlights the 7 services at answer start). `scripts.ts` exports `AI_DEMO_SCRIPT` (14 steps, 29.3s, plan table verbatim), `DEMO_SCRIPTS`, `DEMO_TIME_LIMITS_MS`, `scriptDurationMs`. Tests: highlighted ids exist + named in text, answer fits its 8.5s step, ai ≤ 60s, every step kind has a DemoActions handler, every target is in `DEMO_TARGETS`, full ai run under fake timers never calls `fetch`.
**Key decisions:** `DEMO_SCRIPTS` / `DEMO_TIME_LIMITS_MS` are `Partial<Record<DemoModeName, …>>` — **stage 12** adds `ALL_DEMO_SCRIPT`, `all: 120_000`, and switches both to full `Record` (tests iterate all entries). **Stage 8:** look up the script via `DEMO_SCRIPTS[mode]`; pass `DEMO_SCRIPTED_ANSWER` itself (not a copy) through `playAnswer` to keep AskPanel's identity-keyed effect stable. `closeConnect` step has no `target` (no close-button id in `DEMO_TARGETS`) — pointer stays put. Fake keys `sk-ant-demo-astromart-0000` / `jev-demo-astromart-0000` are display-only. Question kept verbatim from the plan (67 chars, plan said 69; length not asserted).
**User overrides during review:** none.

## Stage 8 — Demo runner and `demo=ai` wiring in App (committed 2026-09-26T10:35:05Z)
**Files:** client/src/demo/useDemoRunner.ts, client/src/demo/__tests__/useDemoRunner.test.ts, client/src/demo/useDemoView.ts, client/src/demo/__tests__/useDemoView.test.ts, client/src/App.tsx
**What was built:** `?demo=ai` skips the intro and plays `AI_DEMO_SCRIPT` end to end via DemoActions (typing at 55ms/char ÷ speed, Connect window fields, fake AI status, Search press, `DEMO_SCRIPTED_ANSWER`). Any trusted click/key aborts: Connect closes, Ask box resets, fake connection dropped, real status check re-runs. `<html data-demo-state>` = running/done/aborted. Demo never writes `cosmos-intro-seen`. `?demo=all` has no script yet → normal load with intro. `useDemoRunner` exposes `status`, `isActive`, `caption`, `target` (sticky until replaced), `isOverlayVisible`; `useDemoView` holds typed question, Search pressed, Connect fields, `isEndCardRequested`.
**Key decisions:** Ask state + both AI hooks lifted from `ProjectCosmosShell` into `App`; `realAi = useAiConnection({ enabled: !isDemoActive && !showIntro && !warping })`. Shell gets `aiConnection`, ask state, `onAsk`/`onAnswerStart`/`onAskAction`, `demoAsk`, `demoConnect` props; `onPlayScenario` prop removed. `DEMO_AWAITING_ANSWER` placeholder opens the panel on `ask` to prevent a live fetch; `playAnswer` swaps in `DEMO_SCRIPTED_ANSWER`. Runner reads latest actions/onEnd via refs; StrictMode double-fires step 0 — **stage 12:** keep `all`'s step 0 idempotent. Stage-12 actions already wired (`pressIntro`, `playScenario`, `stepBack`/`stepForward`, `openIncident`); `toggleLegend` is a no-op (legend state inside `Map`) — **stage 12 must wire it**. **Stage 9:** read `demoRunner.caption`/`.target`/`.isOverlayVisible` in App, use `demoSpeed` for animation scaling; `closeConnect` has no target. **Stage 11:** open `OVERLAY.demoEndCard` from `demoView.view.isEndCardRequested` (survives `reset()`); teardown closes `OVERLAY.connect`/may close `OVERLAY.ask`, not the end card. Not browser-checked (mobile or desktop).
**User overrides during review:** none.
