# Master Run Ledger
Plan: docs/plans/demo-plan.md

## Stage 1 — Demo step types and demo-mode URL parsing (committed 2026-09-26T09:14:31Z)
**Files:** client/src/demo/types.ts, client/src/demo/demoMode.ts, client/src/demo/__tests__/demoMode.test.ts
**What was built:** `types.ts` — full `DemoStep` discriminated union (all §1/§7 kinds, each with `durationMs`, optional `caption`/`target`), `DemoTarget` union, `DemoActions` callback interface, `DemoAiStatus`, `DemoScriptedAnswer`, `DemoMode`/`DemoModeName`. `demoMode.ts` — `readDemoMode` (only `ai`/`all`, speed clamped 1–8, default 1; bad value → null) and `shouldShowIntro(demoMode, storage, hasDeepLink=false)` (never writes storage); exports `INTRO_SEEN_STORAGE_KEY`.
**Key decisions:** `DEMO_TARGETS` / `DEMO_STEP_KINDS` are runtime `as const` arrays; `DEMO_STEP_ACTIONS: Record<DemoStepKind, (keyof DemoActions)[]>` maps kinds to callbacks (for §9 coverage tests). Target ids for stage 10: `intro-start`, `domain-shopping|fulfillment|engagement`, `ask-input`, `ask-search`, `connect-open`, `connect-provider-anthropic|openai`, `connect-provider-key`, `connect-jev-key`, `connect-submit`, `playback-play`, `playback-step-back`, `playback-step-forward`, `legend-ownership`. `toggleLegend{isVisible}` sets explicitly. `ask` kind drives `setSearchPressed` + `ask`; `connect` drives `setAiStatus` (disconnected → connecting → connected). `DemoAiStatus` is separate from app `AiConnectionStatus` (stage 3 maps `connecting` → modal `isBusy`). `DemoScriptedAnswer { text; thinkingMs; wordMs; actions? }` shared by stages 6/7. `demo=all` always shows intro. Stage 8 should pass `hasDeepLink = initial.scenario != null || initial.incident != null || initial.domain != null`.
**User overrides during review:** none.

## Stage 2 — Abortable demo runner with speed scaling (committed 2026-09-26T09:19:14Z)
**Files:** client/src/demo/runDemo.ts, client/src/demo/__tests__/runDemo.test.ts
**What was built:** `runDemo(script, actions, { signal, speed, onStepStart? })` plays steps sequentially through `DemoActions` only, divides durations by `speed`, resolves `'done'`/`'aborted'` (never throws on abort). Exports `sleep(ms, signal)` (clears timer + listener), `scaledDuration(ms, speed)` (for stage 9 animations), and `abortOnTrustedInput(controller, target = window)` (aborts on trusted `pointerdown`/`keydown`, returns cleanup — for stage 8).
**Key decisions:** Call-then-sleep: a step's actions fire at its start, then `durationMs` elapses. `connect` = `setAiStatus('connecting')` → wait → `'connected'`. `ask` = `setSearchPressed(true)` → `min(150, durationMs)` → `setSearchPressed(false)` + `ask(question)` → rest of duration. `onStepStart?(step, stepIndex)` added for stage 9's pointer/caption (runner ignores `target`/`caption`). Abort mid-step skips the closing actions, so stage 8's teardown must reset pressed state and drop the fake AI connection. Non-finite/non-positive speed throws `RangeError`. jsdom `Event.isTrusted` can't be overridden, so `abortOnTrustedInput` takes a `Pick<EventTarget, …>` target and tests pass a fake one.
**User overrides during review:** none.

## Stage 3 — Fake AI connection and disable switch for the real one (committed 2026-09-26T09:23:46Z)
**Files:** client/src/hooks/useAiConnection.ts, client/src/hooks/__tests__/useAiConnection.test.ts, client/src/demo/useDemoAiConnection.ts, client/src/demo/__tests__/useDemoAiConnection.test.ts
**What was built:** `useAiConnection({ enabled })` (default `true`) — when `false`, skips `/api/ai/status` and reports `disconnected`/no provider; re-enabling re-runs the status check. `useDemoAiConnection()` returns an `AiConnection`-compatible fake plus `isConnecting` and `setDemoStatus` (stable `useState` setter); provider always `anthropic` (`DEMO_AI_PROVIDER` exported); `connect`/`disconnect` never touch the network.
**Key decisions:** Mapping `DemoAiStatus` → fields: `disconnected` → `status:'disconnected'`, provider `null`, `isConnecting:false`; `connecting` → `status:'disconnected'`, provider `null`, `isConnecting:true`; `connected` → `status:'connected'`, provider `anthropic`, `isConnecting:false`. **Stage 4:** `ConnectAgentModal` keeps `isBusy` as internal `useState` — needs a new prop (e.g. `isDemoBusy`) so App can pass `demoAi.isConnecting`. **Stage 8:** call both hooks every render: `realAi = useAiConnection({ enabled: !isDemoActive })`, `demoAi = useDemoAiConnection()`, pass `isDemoActive ? demoAi : realAi`; `DemoActions.setAiStatus = demoAi.setDemoStatus`; on end/abort call `demoAi.setDemoStatus('disconnected')` and clear `isDemoActive`. Real hook shows last-known status (possibly `unknown`) after re-enable until the new check returns. Plan's "no `fetch` during full `ai` script" runner test deferred to stage 7 (needs the `demo=ai` script).
**User overrides during review:** none.
