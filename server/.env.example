# Vercel AI Gateway key. Optional: without it, question classification falls back to local relevance
# unless the visitor supplies their own gateway key in the Connect window.
AI_GATEWAY_API_KEY=
# Signs the AI key cookie. 32 random bytes, base64 (openssl rand -base64 32). Without it the AI routes answer 503 AI_NOT_CONFIGURED.
AI_COOKIE_SECRET=
