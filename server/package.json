{
  "name": "@project-cosmos/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p . --noEmit && tsc -p scripts --noEmit",
    "typecheck": "tsc -p . --noEmit && tsc -p scripts --noEmit",
    "test": "vitest run",
    "snapshot": "tsx scripts/snapshot-map.ts"
  },
  "devDependencies": {
    "vitest": "^5.0.2"
  },
  "dependencies": {
    "@langchain/anthropic": "1.5.11",
    "@langchain/core": "1.2.12",
    "@langchain/langgraph": "1.4.18",
    "@langchain/openai": "1.5.13",
    "ai": "^7.0.116",
    "hono": "^4.13.9",
    "zod": "^4.6.5"
  }
}
