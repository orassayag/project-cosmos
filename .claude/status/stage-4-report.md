# Stage 4 report — M0: repoint skills to client/src

## Files
.claude/skills/add-scenario/SKILL.md
.claude/skills/add-service/SKILL.md
.claude/skills/update/SKILL.md
skills/add-scenario/SKILL.md
skills/add-service/SKILL.md

## Summary
- Every Project Cosmos `src/…` path in the skills now reads `client/src/…`. That covers scenario data, `components/`, `styles/tokens.css` and `map/`. It also covers both skill `description` frontmatter lines.
- The verify steps in add-scenario and add-service now run `npm run typecheck`, `npm run snapshot`, `npm run validate`, and `npm run build`. They used to run `npx tsc -b --noEmit`, which no longer works at the root because there is no root tsconfig.
- Both "What you'll touch" tables now list `server/src/generated/cosmos-map.json` as regenerated by `npm run snapshot`, not hand-edited. A short line after each verify block says to commit the snapshot along with the data edits.
- `skills/add-*` are byte-identical copies of `.claude/skills/add-*`, both before and after this change (`diff -r` is clean).
- Gate: every `src/` hit from `grep -rn "src/" .claude/skills skills` is `client/src/…` or `server/src/generated/cosmos-map.json`. The skills had no references to other repos' `src/`, so no hits were ambiguous.
- `npm run validate` passes with 0 errors and no drift. `npm run typecheck` passes. There is no code change, so lint and tests do not apply. All files are 210 lines or fewer.

## Commit message
docs(skills): repoint map-growing skills at client/src

The Vite app moved into client/ and map edits now need a fresh server
snapshot, so the skills' paths and verify steps pointed at a layout that
no longer exists. Keep both skill copies in sync.

## Key decisions
- I included `.claude/skills/update/SKILL.md` even though the brief names only add-service and add-scenario. The stage gate greps all of `.claude/skills`, and this file had 2 stale `src/scenarios`/`src/incidents` references. Its validate step now runs `npm run snapshot` first.
- I replaced `npx tsc -b --noEmit` with `npm run typecheck`. The old command has no root tsconfig to build from after the workspace move. The "never run tsc without --noEmit/-b" hard rule stays.
- Bare filename mentions such as `tokens.css`, `types.ts` and `owners.ts` are unchanged. Each one sits next to a full `client/src/…` path.
