# Stage 3 work brief — M0: server/ workspace skeleton + snapshot-map + committed cosmos-map.json + validate freshness check + apply-edits snapshot hook

Plan: docs/plans/add-ai.md (§1 — Milestone 0, plus the §3 stack line for server tsconfig). Branch: feature/add-ai.

## Stage scope (from the stage plan)
Stage 3: M0: server/ workspace skeleton + snapshot-map.ts + committed cosmos-map.json + validate freshness check + apply-edits snapshot hook ⚠ generated JSON oversized, cannot split further

**In scope:**
- `server/package.json` (workspace `@project-cosmos/server`, ESM `"type": "module"`), with **its own `build` and `typecheck` scripts** (stage 1 fanned root `build`/`typecheck` out with `--workspaces` and NO `--if-present`, so a server workspace without them breaks the root gates). Build for now can be `tsc --noEmit -p .` or equivalent — there is no runtime server code yet (Hono etc. arrives in stage 11). Do NOT add Hono/AI/LangChain deps here. Add a `snapshot` script if it helps (`tsx scripts/snapshot-map.ts`).
- `server/tsconfig.json` with `module`/`moduleResolution: NodeNext` (see §3 stack line below).
- `server/scripts/snapshot-map.ts` — imports only the pure data modules from `client/src/scenarios` (`services`, `topics`, `scenarios`, `steps/*`, `owners`) and `client/src/incidents/data` — **not** `runner.ts` (React hook). Writes compact JSON to `server/src/generated/cosmos-map.json`. Export the snapshot-building function so validate can regenerate in memory and compare. Output must be deterministic (stable key order, trailing newline) so the freshness comparison is exact.
- `server/src/generated/cosmos-map.json` — generated by running the script, then kept in the tree (uncommitted; orchestrator commits).
- Root `package.json`: `workspaces` becomes `["client", "server"]`; add root `snapshot` script. Keep `validate` as the root script `tsx drift-sync/scripts/validate.ts` (not fanned out). `package-lock.json` will change from `npm install` — that's expected.
- `drift-sync/scripts/validate.ts`: add the snapshot-freshness check — regenerate in memory, fail when it differs from the committed file with the exact message `cosmos-map.json is stale — run npm run snapshot`.
- `drift-sync/scripts/apply-edits.ts`: run `npm run snapshot` after it writes edits, and add `server/src/generated/cosmos-map.json` to its `WRITABLE_PATHS`. Also check whether `drift-sync/scripts/sync-nightly.ts` (which has its own `WRITABLE_PATHS` at ~line 213, per the ledger) needs the same path added so the nightly guard doesn't reject the regenerated snapshot — decide by reading how it uses that list and report the decision.
- ESLint: `eslint.config.mjs` currently targets `client/src`; make sure `server/` TS files are linted too if that's a small change (plan: "eslint.config.mjs stays at root, covers both workspaces"). If it's non-trivial, note it in Open questions instead.

**Out of scope (later stages — do NOT touch):**
- `.claude/skills/*`, `skills/*` → stage 4.
- `CLAUDE.md`, root `README.md`, `CONTRIBUTING.md`, `.github/workflows/*` → stages 5/7.
- `vercel.json`, `server/.env.example`, Hono app, routes, tests/Vitest → stages 6, 7, 11+.
- Root `dev` script swap to `vercel dev` → stage 6.

## Plan text (pasted verbatim)

Target layout (§1):
```
├── server/               ← new Node service (see §3)
│   ├── package.json  tsconfig.json
│   ├── src/
│   │   └── generated/cosmos-map.json   ← committed snapshot of the map data (see below)
│   └── scripts/snapshot-map.ts
```

**How the server gets the map data without depending on `client/`.** The map data is the single source of truth, and it stays in `client/src/scenarios/`. drift-sync edits it there, and the skills target it there. `server/scripts/snapshot-map.ts` (run with tsx) imports only the pure data modules (`services`, `topics`, `scenarios`, `steps/*`, `owners`, `incidents/data` — **not** `runner.ts`, which is a React hook) and writes a compact JSON snapshot to `server/src/generated/cosmos-map.json`, which is committed. This keeps `server/` self-contained, so the Vercel server service never reaches outside its root. The snapshot can't silently go stale, for three reasons:
- `npm run validate` regenerates the snapshot in memory and fails when it differs from the committed file, with the message `cosmos-map.json is stale — run npm run snapshot`.
- `drift-sync/scripts/apply-edits.ts` runs `npm run snapshot` after it writes edits, and adds `server/src/generated/cosmos-map.json` to its `WRITABLE_PATHS`.
- `snapshot` is also exposed as a root script.

Root `package.json`: `dev` runs `vercel dev` (both services). `dev:client` keeps plain Vite on :5173. `build`, `lint`, `typecheck`, and `test` fan out with `--workspaces`. `validate` is **not** fanned out: it stays the root script `tsx drift-sync/scripts/validate.ts` (repointed at `client/src/scenarios`), and the snapshot-freshness check lives inside it. No workspace has a `validate` script, so fanning it out would either fail or, with `--if-present`, silently run nothing. **Never use `--if-present` on a gate script.** Dependencies move into the workspace that uses them. `@anthropic-ai/sdk` stays at root for drift-sync.

§3 stack line: **Stack:** a Node service in `server/`. Use Hono on Vercel's Node runtime (Node 24 on Vercel, 22 locally per `.nvmrc`). Set root `engines.node` to `>=22`, because AI SDK 7 requires Node 22+ and ESM. In `server/tsconfig.json`, set `module`/`moduleResolution: NodeNext`. That makes every extensionless relative import a compile error, so the `.js` rule (L016) is enforced by the type-checker instead of discovered at deploy time.

Consumers downstream of the snapshot (for shape guidance — keep all fields they will need): §5 generates `targetScenario` options from `cosmos-map.json` (scenario id → title); `localRelevance` matches service, topic, domain, team, or scenario names; §6 `context.ts` digest covers each service (id, name, domain, owner team, tech, what it talks to), each topic (producers/consumers), each scenario (title plus ordered step summaries), and incidents (title, date, one-line cause); `highlight_services`/`play_scenario` validate against snapshot service ids / scenario ids.

**Verification (subset relevant to this stage)**
- `npm run build`, `npm run lint`, `npm run typecheck`, `npx tsc -p drift-sync --noEmit`, and `npm run validate` all pass.
- `validate` is proven live, not vacuous: … Repeat once with a hand-edited `cosmos-map.json` to see the stale-snapshot failure (exit non-zero, exact message), then regenerate/restore and confirm validate passes again.
- `npm run snapshot` twice in a row produces a byte-identical file (determinism).

## Notes from the ledger
- `npx tsc -p drift-sync --noEmit` was denied by permissions in stages 1 and 2 — try it; if denied again, report it plainly, don't fake it.
- drift-sync/tsconfig.json extends `../client/tsconfig.json`; if validate.ts now imports from `server/scripts/snapshot-map.ts`, confirm drift-sync still type-checks with NodeNext-style `.js` import specifiers in the server file (tsx handles them at runtime; client tsconfig is bundler resolution).
- Pre-existing: `client/src/scenarios/steps/core.ts` (`CORE_STEPS`) is not in the `data.ts` barrel. Build the snapshot from the same barrel the app uses (so snapshot == what the app shows); do not fix the barrel here — mention it if relevant.
- Do NOT run `npm run fresh`.
